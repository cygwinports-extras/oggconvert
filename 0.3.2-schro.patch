schro plugin is now provided by gst-plugins-0.10 instead of with
libschroedinger-1.0.x.  (Pre-1.0 versions of libschroedinger were 0.9,
so this should still rule them out.)

--- origsrc/oggconvert-0.3.2/OggConvert/ocv_init.py	2008-09-12 11:14:11.000000000 -0500
+++ src/oggconvert-0.3.2/OggConvert/ocv_init.py	2009-11-04 01:44:13.779325600 -0600
@@ -37,7 +37,7 @@ if ocv_constants.HAVE_SCHRO is None:
     if gstver[2] > 10:
         schrofac = gst.element_factory_find("schroenc")
         if schrofac is not None:
-            if schrofac.check_version(1,0,0):
+            if schrofac.check_version(0,10,0):
                 print "Schrödinger encoder found, using"
                 ocv_constants.HAVE_SCHRO = True
             else:
